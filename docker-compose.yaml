version: "3.9"

services:
  streamlit-app:
    build: .
    container_name: streamlit_app
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - DB_PATH=/app/database.db
      - APP_SECRET=supersecretkey
    restart: unless-stopped